<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.8.1</version>
            <configuration>
                <source>1.8</source> <!-- Можна замінити на 11, якщо використовуєш Java 11 -->
                <target>1.8</target> <!-- Можна замінити на 11, якщо використовуєш Java 11 -->
            </configuration>
        </plugin>
    </plugins>
</build>
